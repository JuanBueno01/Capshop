<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Recuperar contraseña - CapShop</title>

    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial;
            background-image: url("/img/perro.jpg");
            background-repeat: no-repeat;
            background-position: center;
            background-size: 60%;
        }

        body::after {
            content: "";
            inset: 0;
            position: fixed;
            background: white;
            opacity: 0.8;
            z-index: -1;
        }

        .login-container {
            width: 380px;
            margin: 120px auto;
            padding: 40px;
            border-radius: 20px;
            backdrop-filter: blur(25px);
            background: rgba(255,255,255,0.12);
            box-shadow: 0 8px 30px rgba(0,0,0,0.15);
        }

        input {
            width: 100%;
            padding: 14px;
            margin-top: 15px;
            border-radius: 12px;
            border: none;
            background: rgba(255,255,255,0.85);
        }

        button {
            width: 100%;
            padding: 14px;
            margin-top: 20px;
            border: none;
            border-radius: 12px;
            background: #007bff;
            color: white;
        }

        .link {
            margin-top: 20px;
            text-align: center;
        }

        .link a {
            text-decoration: none;
            font-size: 14px;
        }
    </style>
</head>
<body>

    <div class="login-container">
        <h2>Recuperar contraseña</h2>
        <p class="subtitle">Ingresa tu correo</p>

        <% if (error) { %>
            <p style="color:red; text-align:center"><%= error %></p>
        <% } %>

        <% if (mensaje) { %>
            <p style="color:green; text-align:center"><%- mensaje %></p>
        <% } %>

        <form action="/forgot" method="POST">
            <input type="email" name="correo" placeholder="Correo">
            <button type="submit">Enviar instrucciones</button>
        </form>

        <div class="link">
            <a href="/login">Volver al inicio</a>
        </div>
    </div>

</body>
</html>
