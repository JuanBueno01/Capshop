<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>CapShop - Menú</title>

    <style>
        /* ============================
          FONDO PERRO DIFUMINADO
        ============================ */
        body::before {
          content: "";
          position: fixed;
          top: 0;
          left: 0;
          width: 100%;
          height: 100%;
          background-image: url("/img/perro.jpg");
          background-size: 75%;
          background-position: center;
          background-repeat: no-repeat;
          background-attachment: fixed;
          z-index: -2;
        }

        body::after {
          content: "";
          position: fixed;
          top: 0;
          left: 0;
          width: 100%;
          height: 100%;
          background: white;
          opacity: 0.65;
          z-index: -1;
          pointer-events: none;
        }

        body {
          margin: 0;
          font-family: Arial, sans-serif;
          position: relative;
          z-index: 0;
        }

        /* ============================
          EVITAR SCROLL HORIZONTAL
        ============================ */
        html, body {
            overflow-x: hidden;
        }

        /* ============================
          NAVBAR
        ============================ */
        .navbar-container {
            width: 100%;
            display: flex;
            justify-content: center;
            position: sticky;
            top: 0;
            z-index: 20;
            background: rgba(255, 255, 255, 0.25);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border-bottom: 1px solid rgba(255, 255, 255, 0.4);
        }

        .navbar {
            width: 100%;
            max-width: 1350px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 18px 22px;
            box-sizing: border-box;
        }

        .nav-left {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .logo {
            width: 70px;
            height: 70px;
            object-fit: cover;
            border-radius: 50%;
        }

        .brand {
            font-weight: bold;
            font-size: 26px;
            color: #6a00d7;
        }

        .nav-links {
            display: flex;
            flex: 1;
            justify-content: center;
            gap: 30px;
        }

        .nav-links a {
            text-decoration: none;
            color: #222;
            font-weight: 700;
            font-size: 18px;
            transition: 0.25s ease;
        }

        .nav-links a:hover {
            color: #8a2fff;
        }

        .nav-right {
            white-space: nowrap;
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .user-tag {
            font-size: 16px;
        }

        .btn-logout {
            background: #8a2fff;
            padding: 8px 14px;
            color: white;
            text-decoration: none;
            border-radius: 8px;
            font-weight: bold;
            transition: 0.25s ease;
        }

        .btn-logout:hover {
            background: #a462ff;
        }

        @media (max-width: 820px) {
            .navbar {
                flex-wrap: wrap;
                gap: 12px;
                padding-bottom: 20px;
            }

            .nav-links {
                order: 3;
                width: 100%;
                justify-content: center;
                margin-top: 10px;
                gap: 20px;
            }

            .logo {
                width: 60px;
                height: 60px;
            }
        }

        /* ============================
          BANNER PRINCIPAL
        ============================ */
        .banner {
          text-align: left;
          margin-top: 40px;
          width: 85%;
          margin-left: 40px;
        }

        .banner h1 {
          font-size: 40px;
          color: #111;
          margin-bottom: 10px;
        }

        .banner p {
          color: #555;
          margin-bottom: 20px;
          font-size: 18px;
        }

        .cta {
          padding: 14px 26px;
          background: #6a00d7;
          color: white;
          text-decoration: none;
          border-radius: 10px;
          font-size: 16px;
        }

        /* ============================
          FILTROS
        ============================ */
        .filters {
          margin: 40px auto;
          width: 85%;
          display: flex;
          justify-content: center;
          gap: 15px;
        }

        .filters input,
        .filters select {
          padding: 12px;
          border-radius: 10px;
          border: 1px solid #aaa;
        }

        .filters input {
          width: 300px;
        }

        /* ============================
          GRID DE PRODUCTOS
        ============================ */
        .grid {
          width: 90%;
          margin: auto;
          display: grid;
          grid-template-columns: repeat(4, 220px);
          justify-content: center;
          gap: 25px;
          padding-bottom: 50px;
        }

        .card {
          background: white;
          border-radius: 15px;
          padding: 15px;
          text-align: left;
          box-shadow: 0 5px 15px rgba(0,0,0,0.1);
        }

        .card img {
          width: 100%;
          height: 180px;
          object-fit: contain;
          border-radius: 12px;
        }

        .card h3 {
          margin: 10px 0 5px;
        }

        .price {
          color: #6a00d7;
          font-weight: bold;
        }

        .card-buttons {
          display: flex;
          gap: 10px;
          margin-top: 12px;
        }

        .btn-add,
        .btn-detail {
          padding: 10px;
          border-radius: 8px;
          cursor: pointer;
          border: none;
        }

        .btn-add {
          width: 100%;
          background: #6a00d7;
          color: white;
        }

        .btn-detail {
          background: white;
          border: 2px solid #6a00d7;
          color: #6a00d7;
          text-align: center;
          width: 100%;
          display: inline-block;
          text-decoration: none;
        }
    </style>
</head>

<body>

    <!-- NAVBAR -->
    <header class="navbar-container">
    <div class="navbar">

        <div class="nav-left">
            <img src="/img/perro.jpg" class="logo">
            <h2 class="brand">CapXhop</h2>
        </div>

        <nav class="nav-links">
            <a href="/">Inicio</a>
            <a href="/menu">Catálogo</a>
            <a href="/carrito">Carrito</a>
        </nav>

        <div class="nav-right">
            <span class="user-tag">Bienvenido, <%= userName %></span>
            <a href="/logout" class="btn-logout">Cerrar sesión</a>
        </div>

    </div>
</header>

    <!-- BANNER -->
    <section class="banner">
        <h1>Gorras para todos los estilos</h1>
        <p>Envío gratis en compras superiores a $52.900</p>
        <a class="cta" href="#grid">Ver catálogo</a>
    </section>

    <!-- SEARCH / FILTER BAR -->
    <section class="filters">
        <input type="text" placeholder="Buscar gorras, marca o categoría...">
        <select><option>Categoría</option></select>
        <select><option>Precio</option></select>
        <select><option>Más populares</option></select>
    </section>

    <!-- PRODUCT GRID -->
    <section id="grid" class="grid">

        <% products.forEach(p => { %>
            <div class="card">

                <img src="/img/<%= p.imagen %>" alt="<%= p.nombre %>">

                <h3><%= p.nombre %></h3>

                <p class="price">$<%= p.precio %></p>

                <p class="desc"><%= p.descripcion %></p>

                <div class="card-buttons">

                    <div style="width: 50%;">
                        <form action="/add-to-cart/<%= p.id_producto %>" method="POST">
                            <button type="submit" class="btn-add">Agregar al carrito</button>
                        </form>
                    </div>

                    <a href="/product/<%= p.id_producto %>" class="btn-detail">
                        Ver detalle
                    </a>

                </div>

            </div>
        <% }) %>

    </section>

</body>
</html>
