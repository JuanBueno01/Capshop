<!DOCTYPE html>
<html lang="es">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Restablecer Contraseña CapShop</title>
<style>
    body {
        margin: 0;
        padding: 0;
        background: #fff;
        font-family: Arial, sans-serif;
        background-image: url("/img/perro.jpg");
        background-repeat: no-repeat;
        background-position: center center;
        background-size: 60%;
    }
    body::after {
        content: "";
        position: fixed;
        top:0;
        left:0;
        width:100%;
        height:100%;
        background: white;
        opacity:0.8;
        pointer-events: none;
        z-index: -1;
    }
    .login-container {
        position: relative;
        width: 380px;
        margin: 120px auto;
        padding: 40px;
        border-radius: 20px;
        backdrop-filter: blur(25px);
        background: rgba(255,255,255,0.116);
        box-shadow: 0 8px 30px rgba(0,0,0,0.15);
    }
    .form-content { position: relative; text-align: left; }
    .form-content h2 { margin:0 0 10px 0; font-size:26px; font-weight:bold; }
    .subtitle { font-size:14px; margin-bottom:20px; color:#666; opacity:0.6; }
    .form-content input { width:100%; padding:14px; margin-top:15px; border-radius:12px; border:none; outline:none; background:rgba(255,255,255,0.85); font-size:15px; }
    .form-content button { width:100%; padding:14px; margin-top:20px; border-radius:12px; border:none; background:#007bff; color:#fff; font-size:16px; cursor:pointer; }
    .form-content button:hover { opacity:.9; }
    .back-link { margin-top:15px; text-align:center; }
    .back-link a { font-size:14px; color:#0056d6; text-decoration:none; opacity:0.9; display:block; }
    .back-link a:hover { text-decoration:underline; opacity:1; }
    .mensaje { color: red; font-size: 14px; margin-top: 10px; text-align: center; }
    .mensaje-exito { color: green; font-size: 14px; margin-top: 10px; text-align: center; }
</style>
</head>
<body>
<div class="login-container">
    <div class="form-content">
        <h2>Restablecer Contraseña</h2>
        <p class="subtitle">Ingresa tu nueva contraseña</p>

        <% if (typeof error !== 'undefined' && error) { %>
            <div class="mensaje"><%= error %></div>
        <% } %>

        <% if (typeof mensaje !== 'undefined' && mensaje) { %>
            <div class="mensaje-exito"><%= mensaje %></div>
        <% } %>

        <form action="/reset-password/<%= token %>" method="POST">
            <input type="password" name="contrasena" placeholder="Nueva contraseña" required>
            <button type="submit">Cambiar contraseña</button>
        </form>

        <div class="back-link">
            <a href="/login">Volver al login</a>
        </div>
    </div>
</div>
</body>
</html>
